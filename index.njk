---
layout: main.njk
title: Mohit Karekar
---
<style>
.header {
    display: flex; 
    justify-content: space-between;
}
.headImage {
    margin-right: 0
}
.mainLine {
    margin: 0;
    font-size: 2em;
}
.shortBio {
    margin-top: 0px;
    opacity: 0.8;
}
.mainCover {
    position: relative;
    margin: 20px 0;
    max-height: 300px;
    overflow: hidden;
}
.mainCover img {
    max-width: 100%;
    display: block;
}
.photoCaption {
    position: absolute;
    bottom: 0px;
    right: 20px;
    color: white;
    font-weight: bold;
    filter: drop-shadow(2px 4px 10px #333);
}
.tabs {
    margin-top: 10px;
}
.tab {
    font-size: 20px;
}
.down {
    display: flex;
}
section {
    padding: 10px;
    max-height: 250px;
    overflow-y: auto;
}
.downLeft {
    flex: 0.6;
    margin-right: 10px;
    background: #fee;
}
.downLeft h3, .downRight h3, .interesting h3 {
    margin: 0;
}
.downRight {
    flex: 0.4;
    background: #dff;
}
.post {
    margin-top: 10px;
}
.interesting {
    background: #efe;
    margin-top: 10px;
}
.socials {
    margin-top: 10px;
}
.social {
    max-width: 30px;
    margin-left: 10px;
    transition: all 200ms ease-in;
}
.social:hover {
    opacity: 0.8;
}
.detailedBlogs {
    display: flex;
    flex-wrap: wrap;
}
.detailedBlog {
    width: 100%;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    padding: 10px;
    transition: all 200ms ease-in-out;
    cursor: pointer;
}
.detailedBlog:hover {
    background: #f2f2f2;
}
.detailedBlog h4 {
    margin: 5px 0;
}
.detailedBlogImage {
    display: block;
    height: 100px;
    width: 200px;
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
}
.detailedBlog-description {
    margin: 5px 0;
}
button {
    background: #f5f5f5;
    border: none;
    font-size: 1em;
    padding: 10px 10px;
    cursor: pointer;
    transition: all 200ms ease-in-out;
}
button:hover {
    background: #eee;
}
@media only screen and (max-width: 768px) {
    .down {
        flex-direction: column;
    }
    .downLeft {
        margin-right: 0;
        margin-bottom: 10px;
    }
    .header {
        flex-direction: column;
    }
    .social {
        margin-left: 0;
        margin-right: 10px;
    }
    .mainCover {
        max-height: 200px;
    }
}
</style>
<div style="margin-bottom: 20px;">
{%- for cover in covers | randomSingle -%}
<div class="mainCover">
    <img lazy alt="Cover" src="{{cover.url}}" />
    <p class="photoCaption">{{cover.caption}}</p>
</div>
{%- endfor -%}
<div class="header">
    <div class="head">
        <h1 class="mainLine">Mohit Karekar</h1>
        <p class="shortBio">software engineer, javascript, open source | experimenting with rust</p>
    </div>
    <div class="socials">
        <a target="_blank" href="https://github.com/mohitk05"><img alt="github" class="social" src="/img/github.png"/></a>
        <a target="_blank" href="https://twitter.com/mohitkarekar"><img alt="twitter" class="social" src="/img/twitter.png"/></a>
    </div>
</div>
<div class="tabs">
    <a class="tab" href="/posts">Blog</a>
    •
    <a class="tab" href="/work">Work</a>
    •
    <a class="tab" target="_blank" href="https://docs.google.com/document/d/1239BbrB9jy8trrCkwhFwJuxBy8iwHrEd62rVPXk_Zrg/edit?usp=sharing">Resume</a>
</div>
<p></p>
<div class="down">
    <section class="downLeft">
        <h3>Recent posts</h3>
        <div class="posts">
            {%- for post in collections.posts | reverse | slice3 -%}
                <div class="post">
                    <a class="postTitle" href="{{ post.url }}">{{ post.data.title }}</a>
                </div>
            {%- endfor -%}
        </div>
    </section>
    <section class="downRight">
        <h3>Currently reading</h3>
        <div class="posts">
            {%- for c in currentlyReading | reverse | slice3 -%}
                <div class="post">
                    <a class="postTitle" target="_blank" href="{{ c.url }}">{{ c.title }}</a>
                    {% if c.pinned %}
                        <span>📌</span>
                    {% endif %}
                </div>
            {%- endfor -%}
        </div>
    </section>
</div>
<section class="interesting">
    <h3>Interesting things I found on the Internet</h3>
    <div class="posts">
        {%- for c in interesting | reverse -%}
            <div class="post">
                <a class="postTitle" target="_blank" href="{{ c.url }}">{{ c.title }}</a>
            </div>
        {%- endfor -%}
    </div>
</section>
<div>
    <h2 style="padding-left: 10px;">Posts</h2>
    <div class="detailedBlogs">
    {%- for post in collections.posts | reverse -%}
        <div class="detailedBlog" onclick="window.location = '{{post.url}}'">
            <h4 style="margin: 0">{{ post.data.title }}</h4>
            <span style="color: grey; font-size: 0.8em">{{ post.data.date | dateReadable }}</span>
            {% if post.template.frontMatter.excerpt %}
            <p class="detailedBlog-description">{{ post.template.frontMatter.excerpt | excerptise }}</p>
            {% else %}
            <p class="detailedBlog-description">{{ post.data.description | excerptise }}</p>
            {% endif %}
        </div>
    {%- endfor -%}
    </div>
</div>
</div>
